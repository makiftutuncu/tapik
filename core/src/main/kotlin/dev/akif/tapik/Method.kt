package dev.akif.tapik

/** Standard HTTP methods supported by Tapik's endpoint DSL. */
enum class Method {
    GET,
    HEAD,
    POST,
    PUT,
    PATCH,
    DELETE,
    CONNECT,
    OPTIONS,
    TRACE,
    QUERY
}
