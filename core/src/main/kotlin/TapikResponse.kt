package dev.akif.tapik

/**
 * Marker interface implemented by generated endpoint response hierarchies.
 *
 * Integrations can use this marker to recognize Tapik-managed response values and convert them into
 * framework-specific response types without relying on reflection over arbitrary user models.
 */
interface TapikResponse
