- [ ] data class properties of types LocalDate and Instant were unable to get derived with Jackson. i had to manually define schemas for all my types in that case. default types should already work and it should be possible to register custom schemas so i can wire in my custom schema and don't have to manually define everything
- [ ] conflicting openapi component error message is not helpful in understanding what exactly is conflicting
- [ ] `Header(...)` uses smart constructor, which doesn't include documentation parameters so it's not possible to document headers from public api. Same seems to happen for QueryParameter and PathVariable
- [ ] `id` of `Api` causes issues when I want to have an `id` property in my Api object to refer to a resource id path variable. `Api#id` should probably be named something else that wouldn't conflict with users' common use cases (or not be public if possible). What's worse is, because `Api#id` is a string, it becomes perfectly valid to do things like `val get by get(root / id)` where `id` refers to the id of the Api and not a property i defined myself for a common path variable.
- [ ] `Status` is not exhaustive. make it so and make it possible to build a custom one within ranges from an int
- [ ] `Header` should contain common values similar to Spring like `Header.Authorization` etc.
- [ ] `MediaType` should contain common values similar to Spring and i should be able to build custom ones
- [ ] Defaults interfaces should provide a member for enums, so it should be possible to do `StringFormat.enum<MyEnum>()` for example whose implementation could be `StringFormat.string.transformOrThrow(decode = { MyEnum.valueOf(it) }, encode = { it.name })`
- [ ] having generated a server interface and controller, i ran a spring boot app but it didn't fail due to missing handler bean definition. adding `@EnableTapikWebMvc` doesn't change anything. when i actually implement a handler and mark it as `@Component` it works as expected. this seems to have to do with springs component scan because generated classes are in the wrong package. recent changes to avoid conflicts seems to have broken this expectation that generated classes will also be part of host spring application's component scan
- both server and client code generates types for input and outputs of endpoints. they should ideally be generated separately and outside of actual generated interface so if a project generates both server and client from same endpoint, the response types they refer to are actually the same types (helps build proxies of same endpoints, generated server uses generated client in its implementation, currently generating same types separately causes errors like "Required type: generated.com.formident.api.Patients.PatientsServer.ListResponse Provided: generated.com.formident.api.Patients.PatientsClient.ListResponse")
- [ ] 
