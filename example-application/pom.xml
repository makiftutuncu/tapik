<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.akif</groupId>
        <artifactId>tapik-parent</artifactId>
        <version>0.6.0</version>
    </parent>

    <artifactId>tapik-example-application</artifactId>
    <name>tapik runnable library example</name>

    <properties>
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>dev.akif</groupId>
                <artifactId>tapik-bom</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- tag::compiled-contract-dependency[] -->
        <dependency>
            <groupId>dev.akif</groupId>
            <artifactId>tapik-example-contract</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- end::compiled-contract-dependency[] -->
        <dependency>
            <groupId>dev.akif</groupId>
            <artifactId>tapik-target-spring-restclient</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.akif</groupId>
            <artifactId>tapik-target-spring-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-restclient</artifactId>
        </dependency>
        <dependency>
            <groupId>io.kotest</groupId>
            <artifactId>kotest-runner-junit5-jvm</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.kotest</groupId>
            <artifactId>kotest-assertions-core-jvm</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <version>${kotlin.version}</version>
            </plugin>
            <plugin>
                <groupId>dev.akif</groupId>
                <artifactId>tapik-plugin-maven</artifactId>
                <version>${project.version}</version>
                <executions>
                    <!-- tag::compiled-contract-openapi[] -->
                    <execution>
                        <id>generate-openapi</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <target>openapi</target>
                            <targetConfiguration>
                                <version>${project.version}</version>
                            </targetConfiguration>
                        </configuration>
                    </execution>
                    <!-- end::compiled-contract-openapi[] -->
                    <execution>
                        <id>generate-filtered-openapi</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <target>openapi</target>
                            <includeApis>
                                <includeApi>Authors</includeApi>
                                <includeApi>Catalog</includeApi>
                            </includeApis>
                            <excludeApis>
                                <excludeApi>Authors</excludeApi>
                            </excludeApis>
                            <targetConfiguration>
                                <version>${project.version}</version>
                                <output>filtered/{api}.yml</output>
                            </targetConfiguration>
                        </configuration>
                    </execution>
                    <execution>
                        <id>generate-restclient</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <target>spring-restclient</target>
                            <outputDirectory>${project.build.directory}/generated-sources/tapik-restclient</outputDirectory>
                            <targetConfiguration>
                                <packageName>com.example.library.generated</packageName>
                            </targetConfiguration>
                        </configuration>
                    </execution>
                    <execution>
                        <id>generate-webmvc</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <target>spring-webmvc</target>
                            <outputDirectory>${project.build.directory}/generated-sources/tapik-webmvc</outputDirectory>
                            <targetConfiguration>
                                <packageName>com.example.library.generated</packageName>
                            </targetConfiguration>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
