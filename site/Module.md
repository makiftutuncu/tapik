# Module tapik

tapik defines type-safe HTTP APIs as ordinary Kotlin values and turns those contracts into OpenAPI documents, Spring
RestClient clients, and Spring WebMVC servers at build time.

This reference combines every published tapik module. Start with the `dev.akif.tapik` package for the core DSL, then
follow the `format`, `target`, and `plugin` packages for optional integrations.
