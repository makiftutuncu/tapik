{
  "name": "tapik-documentation",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "docs:prepare-ui": "node site/scripts/prepare-antora-ui.mjs",
    "docs:test": "node --test site/scripts/*.test.mjs",
    "docs:integrate-api": "node site/scripts/integrate-dokka-reference.mjs",
    "docs:preview": "npm run docs:test && npm run docs:prepare-ui && npm run docs:integrate-api && antora antora-preview-playbook.yml",
    "docs:build": "npm run docs:test && npm run docs:prepare-ui && npm run docs:integrate-api && antora antora-playbook.yml"
  },
  "devDependencies": {
    "@antora/cli": "3.2.0",
    "@antora/collector-extension": "1.0.3",
    "@antora/lunr-extension": "1.0.0-alpha.13",
    "@antora/site-generator": "3.2.0"
  }
}
